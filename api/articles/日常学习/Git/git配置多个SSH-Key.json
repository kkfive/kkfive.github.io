{"title":"git配置多个SSH-Key","slug":"日常学习/Git/git配置多个SSH-Key","date":"2020-06-23T14:03:00.000Z","updated":"2022-02-21T07:18:57.041Z","comments":true,"path":"api/articles/日常学习/Git/git配置多个SSH-Key.json","excerpt":null,"covers":null,"content":"<h2 id=\"前言\"><a class=\"markdownIt-Anchor\" href=\"#前言\"></a> 前言</h2>\n<p>由于Coding日常爆炸，不得不考虑所有站点都进行多部署操作了。但是在其他平台最好的方式就是通过GitHub作为媒介，进行无缝同步。于是为了个人仓库看起。来不是那么混乱，于是决定在注册一个GitHub账号，专门用来存放静态页面。作为多部署的媒介（GitHub Pages太慢了，只能放弃）。</p>\n<p>那么问题来了，我的ssh公钥已经在我的主账号添加了，小号不能在添加这个公钥。于是只能新创建一个密钥对。但是无论如何，小号都无法通过验证。</p>\n<pre class=\"line-numbers language-bash\" data-language=\"bash\"><code class=\"language-bash\">ssh-keygen -t rsa -C \"examp@examp.com” -f ~/.ssh/github-gos967<span aria-hidden=\"true\" class=\"line-numbers-rows\"><span></span></span></code></pre>\n<h2 id=\"解决方案\"><a class=\"markdownIt-Anchor\" href=\"#解决方案\"></a> 解决方案</h2>\n<p>通过向百度学习，找到了一种较为妥协的方案。</p>\n<p>修改<code>~/.ssh/config</code>文件，配置git信息</p>\n<pre class=\"line-numbers language-none\"><code class=\"language-none\"># 账号1登陆认证\nHost github.com\n    HostName github.com\n    IdentityFile ~\\\\.ssh\\\\id_rsa\n\n# 账号2的登陆认证\nHost tzki.github.com\n    HostName github.com\n    IdentityFile ~\\\\.ssh\\\\gos967_rsa<span aria-hidden=\"true\" class=\"line-numbers-rows\"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>\n<blockquote>\n<ul>\n<li>Host表示别名，随便设置即可。</li>\n<li>HostName表示服务器的地址。</li>\n<li>IdentityFile即表示自定义文件</li>\n</ul>\n</blockquote>\n<p>这里第一个设置表示链接服务器<code>github.com</code>（别名）这个服务器时，使用id_rsa密钥。</p>\n<p>第二个设置表示链接服务器<code>tzki.github.com</code>时使用gos967_rsa密钥。</p>\n<p>接下来只需要将小号的地址修改为<code>git@tzki.github.com:xxx/xxx.git</code>即可。（原地址<code>git@github.com:xxx/xxx.git</code>）</p>\n","more":"<h2 id=\"前言\"><a class=\"markdownIt-Anchor\" href=\"#前言\"></a> 前言</h2>\n<p>由于Coding日常爆炸，不得不考虑所有站点都进行多部署操作了。但是在其他平台最好的方式就是通过GitHub作为媒介，进行无缝同步。于是为了个人仓库看起。来不是那么混乱，于是决定在注册一个GitHub账号，专门用来存放静态页面。作为多部署的媒介（GitHub Pages太慢了，只能放弃）。</p>\n<p>那么问题来了，我的ssh公钥已经在我的主账号添加了，小号不能在添加这个公钥。于是只能新创建一个密钥对。但是无论如何，小号都无法通过验证。</p>\n<pre class=\"line-numbers language-bash\" data-language=\"bash\"><code class=\"language-bash\">ssh-keygen -t rsa -C \"examp@examp.com” -f ~/.ssh/github-gos967<span aria-hidden=\"true\" class=\"line-numbers-rows\"><span></span></span></code></pre>\n<h2 id=\"解决方案\"><a class=\"markdownIt-Anchor\" href=\"#解决方案\"></a> 解决方案</h2>\n<p>通过向百度学习，找到了一种较为妥协的方案。</p>\n<p>修改<code>~/.ssh/config</code>文件，配置git信息</p>\n<pre class=\"line-numbers language-none\"><code class=\"language-none\"># 账号1登陆认证\nHost github.com\n    HostName github.com\n    IdentityFile ~\\\\.ssh\\\\id_rsa\n\n# 账号2的登陆认证\nHost tzki.github.com\n    HostName github.com\n    IdentityFile ~\\\\.ssh\\\\gos967_rsa<span aria-hidden=\"true\" class=\"line-numbers-rows\"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>\n<blockquote>\n<ul>\n<li>Host表示别名，随便设置即可。</li>\n<li>HostName表示服务器的地址。</li>\n<li>IdentityFile即表示自定义文件</li>\n</ul>\n</blockquote>\n<p>这里第一个设置表示链接服务器<code>github.com</code>（别名）这个服务器时，使用id_rsa密钥。</p>\n<p>第二个设置表示链接服务器<code>tzki.github.com</code>时使用gos967_rsa密钥。</p>\n<p>接下来只需要将小号的地址修改为<code>git@tzki.github.com:xxx/xxx.git</code>即可。（原地址<code>git@github.com:xxx/xxx.git</code>）</p>\n","categories":[{"name":"综合学习","path":"api/categories/综合学习.json"},{"name":"SSH","path":"api/categories/SSH.json"}],"tags":[{"name":"SSH-key","path":"api/tags/SSH-key.json"}]}